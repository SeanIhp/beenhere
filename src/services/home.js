import request from '../utils/request';
// import { SERVICE_WXPROXY,SERVICE_TENANT } from '../constants';

export async function getMerchantSummary(reqdata) {
  // return request(SERVICE_TENANT + '/rest?method=efuture.omp.tenant.homepage.query',{
  //   method: 'post',
  //   body: JSON.stringify(reqdata)
  // });
}

export async function getEnterprise(reqdata) {
  // return request(SERVICE_TENANT + '/rest?method=efuture.omp.tenant.homepage.query',{
  //   method: 'post',
  //   body: JSON.stringify(reqdata)
  // });
}